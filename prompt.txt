寫一個iOS的app, 是可以檢查特定網址/port/ip address的連接狀態，而且有widget功能，widget是顯示連接狀態的列表，要一頁顯示幾個，不要一頁一個

widget寫了尚未添加伺服器，但main app已經添加了

widget的padding少一點

為這個app設計一個app icon

為這個app 加入語言選項，語言預設英文
為這個app 加入refresh時間選項，預設1分鐘

要預設多國語言，什麼語言都要有

介面加入版本號

右上角的兩個按鈕可以分得開一點，以及左右padding多一點

refresh button的左padding要和add button 的右padding一樣

widget的上下padding可以減少

2x2 (Medium) widget 標題改為 "Server" / "Servers"（根據數量單複數）
2x4 (Large) widget 最多顯示 6 個伺服器

加入export和import function

import Servers — 選擇 JSON 檔案後，會詢問「合併」或「取代全部」
有什麼分別？

說明的文字要寫得清楚一點，所有語言都要有

顯示server latency數值

widget都要改

主程式要顯示對上一次更新的秒數